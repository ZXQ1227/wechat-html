# wechat-html-v13

微信公众号文章 HTML 排版技能（v13）。将任意 Markdown / 纯文本文章转为微信公众号兼容的内联样式 HTML，AI 自动分析文章题材、情感基调、内容类型，从 20+ 风格族中智能匹配最佳排版风格，直接输出可粘贴到微信后台的 HTML 代码。

## Skill Description For Claude

将文章排版为微信公众号兼容 HTML。读取 `{baseDir}/prompt.md` 作为完整排版规范，按其中的「自动匹配系统」分析文章特征并选择风格，直接输出 HTML 代码。当用户说「排版」「HTML排版」「转微信格式」「用v13排版」时使用。

## 提示词位置

完整排版规范（风格定义 + 自动匹配系统 + 输出要求）存放在：

```
{baseDir}/prompt.md
```

执行时必须先读取该文件，再按其中规则处理文章。

## Instructions

### 触发条件

用户说以下任意一种：
- `排版这篇文章`
- `用v13排版`
- `HTML排版`
- `转微信格式`
- `/wechat-html-v13 文件路径`

### 工作流

#### 第 1 步：获取文章

1. 如果用户给了文件路径，用 Read 工具读取文件
2. 如果用户直接粘贴了文章内容，直接使用
3. 如果都没有，询问文章路径或内容

#### 第 2 步：读取排版规范

读取 `{baseDir}/prompt.md`，这是完整的排版规范，包含：
- 20+ 风格族的配色方案和 HTML 结构模板
- 自动匹配系统（特征提取 → 风格推荐）
- 高分模板优先原则
- 输出格式要求

#### 第 3 步：分析文章特征

按 prompt.md 中「第一步：分析文章特征」的维度提取：
- 题材领域
- 内容类型
- 情感基调
- 内容形式
- 目标人群

#### 第 4 步：推荐风格

按 prompt.md 中「第二步：文章特征 → 风格匹配表」打分，输出：

```
【风格推荐】
🥇 推荐风格：X（风格名）
   匹配理由：...

🥈 备选风格：Y（风格名）
   备选理由：...

🥉 第三选择：Z（风格名）
   理由：...
```

询问用户确认使用哪个风格，或直接使用推荐风格。

#### 第 5 步：生成 HTML

按 prompt.md 中该风格的配色方案和核心结构生成完整 HTML，要求：
- 所有样式为内联 `style="..."` 属性，无外部 CSS
- 直接输出 HTML 代码块，无需额外解释
- 严格遵循 prompt.md 中「重要：请直接输出HTML代码，不需要额外解释」的要求

#### 第 6 步：告知复制方式

输出完成后告知用户：
> HTML 已生成，复制代码块内容，粘贴到微信公众号后台「图文消息」编辑器的 HTML 模式即可。

### 注意事项

- 优先选择审美评分高的风格（S/L/M/Q/R/G/J 等高分组）
- 同等条件下不要为了「差异化」选择低分模板
- 文章超过 3000 字时，添加超长文章处理结构
- 图片用占位符 `[图片]` 标注，告知用户上传后替换
